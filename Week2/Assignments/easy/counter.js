let counter = 0;

setInterval(() => {
  counter++;
  console.log("Counter:", counter);
}, 1000); // Repeat every 1000


// setInterval executes the function at every specific interval of time provided in arguments , suppose argument passed is 3 seconds, so it will run function again again at every 3 seconds time interval


